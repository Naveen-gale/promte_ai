# Example environment variables for the frontend
# Rename this file to .env.local for local overrides
# Set these as Environment Variables in your Vercel project dashboard

# URL of your deployed Render backend (no trailing slash)
# Leave blank to use the Vite dev-proxy (localhost:5000)
VITE_API_URL=https://your-backend.onrender.com
